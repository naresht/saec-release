// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.bfds.saec.domain;

import com.bfds.saec.domain.LetterCode;
import com.bfds.saec.domain.LetterCodeDataOnDemand;
import com.bfds.saec.domain.WorkType;
import com.bfds.saec.domain.reference.LetterType;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect LetterCodeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: LetterCodeDataOnDemand: @Component;
    
    private Random LetterCodeDataOnDemand.rnd = new SecureRandom();
    
    private List<LetterCode> LetterCodeDataOnDemand.data;
    
    public void LetterCodeDataOnDemand.setActiveForEvent(LetterCode obj, int index) {
        Boolean activeForEvent = false;
        obj.setActiveForEvent(activeForEvent);
    }
    
    public void LetterCodeDataOnDemand.setCode(LetterCode obj, int index) {
        String code = "code_" + index;
        obj.setCode(code);
    }
    
    public void LetterCodeDataOnDemand.setDescription(LetterCode obj, int index) {
        String description = "description_" + index;
        obj.setDescription(description);
    }
    
    public void LetterCodeDataOnDemand.setLetterType(LetterCode obj, int index) {
        LetterType letterType = LetterType.class.getEnumConstants()[0];
        obj.setLetterType(letterType);
    }
    
    public void LetterCodeDataOnDemand.setWorkType(LetterCode obj, int index) {
        WorkType workType = null;
        obj.setWorkType(workType);
    }
    
    public LetterCode LetterCodeDataOnDemand.getSpecificLetterCode(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        LetterCode obj = data.get(index);
        Long id = obj.getId();
        return LetterCode.findLetterCode(id);
    }
    
    public LetterCode LetterCodeDataOnDemand.getRandomLetterCode() {
        init();
        LetterCode obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return LetterCode.findLetterCode(id);
    }
    
    public boolean LetterCodeDataOnDemand.modifyLetterCode(LetterCode obj) {
        return false;
    }
    
    public void LetterCodeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = LetterCode.findLetterCodeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'LetterCode' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<LetterCode>();
        for (int i = 0; i < 10; i++) {
            LetterCode obj = getNewTransientLetterCode(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
