// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.bfds.saec.domain;

import com.bfds.saec.domain.CorrespondenceDocument;
import com.bfds.saec.domain.CorrespondenceDocumentDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect CorrespondenceDocumentDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CorrespondenceDocumentDataOnDemand: @Component;
    
    private Random CorrespondenceDocumentDataOnDemand.rnd = new SecureRandom();
    
    private List<CorrespondenceDocument> CorrespondenceDocumentDataOnDemand.data;
    
    public CorrespondenceDocument CorrespondenceDocumentDataOnDemand.getNewTransientCorrespondenceDocument(int index) {
        CorrespondenceDocument obj = new CorrespondenceDocument();
        setName(obj, index);
        setSelectedMissingDocs(obj, index);
        setWithinThreshold(obj, index);
        return obj;
    }
    
    public void CorrespondenceDocumentDataOnDemand.setName(CorrespondenceDocument obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public void CorrespondenceDocumentDataOnDemand.setSelectedMissingDocs(CorrespondenceDocument obj, int index) {
        Boolean selectedMissingDocs = false;
        obj.setSelectedMissingDocs(selectedMissingDocs);
    }
    
    public void CorrespondenceDocumentDataOnDemand.setWithinThreshold(CorrespondenceDocument obj, int index) {
        Boolean withinThreshold = Boolean.TRUE;
        obj.setWithinThreshold(withinThreshold);
    }
    
    public CorrespondenceDocument CorrespondenceDocumentDataOnDemand.getSpecificCorrespondenceDocument(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        CorrespondenceDocument obj = data.get(index);
        Long id = obj.getId();
        return CorrespondenceDocument.findCorrespondenceDocument(id);
    }
    
    public CorrespondenceDocument CorrespondenceDocumentDataOnDemand.getRandomCorrespondenceDocument() {
        init();
        CorrespondenceDocument obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return CorrespondenceDocument.findCorrespondenceDocument(id);
    }
    
    public boolean CorrespondenceDocumentDataOnDemand.modifyCorrespondenceDocument(CorrespondenceDocument obj) {
        return false;
    }
    
    public void CorrespondenceDocumentDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = CorrespondenceDocument.findCorrespondenceDocumentEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'CorrespondenceDocument' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<CorrespondenceDocument>();
        for (int i = 0; i < 10; i++) {
            CorrespondenceDocument obj = getNewTransientCorrespondenceDocument(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
