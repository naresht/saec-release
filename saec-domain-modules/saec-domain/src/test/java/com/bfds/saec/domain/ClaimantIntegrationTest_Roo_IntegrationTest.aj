// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.bfds.saec.domain;

import com.bfds.saec.domain.Claimant;
import com.bfds.saec.domain.ClaimantDataOnDemand;
import com.bfds.saec.domain.ClaimantIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ClaimantIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ClaimantIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ClaimantIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ClaimantIntegrationTest: @Transactional;
    
    @Autowired
    private ClaimantDataOnDemand ClaimantIntegrationTest.dod;
    
    @Test
    public void ClaimantIntegrationTest.testCountClaimants() {
        Assert.assertNotNull("Data on demand for 'Claimant' failed to initialize correctly", dod.getRandomClaimant());
        long count = Claimant.countClaimants();
        Assert.assertTrue("Counter for 'Claimant' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ClaimantIntegrationTest.testFindClaimant() {
        Claimant obj = dod.getRandomClaimant();
        Assert.assertNotNull("Data on demand for 'Claimant' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Claimant' failed to provide an identifier", id);
        obj = Claimant.findClaimant(id);
        Assert.assertNotNull("Find method for 'Claimant' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Claimant' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ClaimantIntegrationTest.testFindAllClaimants() {
        Assert.assertNotNull("Data on demand for 'Claimant' failed to initialize correctly", dod.getRandomClaimant());
        long count = Claimant.countClaimants();
        Assert.assertTrue("Too expensive to perform a find all test for 'Claimant', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Claimant> result = Claimant.findAllClaimants();
        Assert.assertNotNull("Find all method for 'Claimant' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Claimant' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ClaimantIntegrationTest.testFindClaimantEntries() {
        Assert.assertNotNull("Data on demand for 'Claimant' failed to initialize correctly", dod.getRandomClaimant());
        long count = Claimant.countClaimants();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Claimant> result = Claimant.findClaimantEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Claimant' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Claimant' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ClaimantIntegrationTest.testFlush() {
        Claimant obj = dod.getRandomClaimant();
        Assert.assertNotNull("Data on demand for 'Claimant' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Claimant' failed to provide an identifier", id);
        obj = Claimant.findClaimant(id);
        Assert.assertNotNull("Find method for 'Claimant' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyClaimant(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Claimant' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ClaimantIntegrationTest.testMergeUpdate() {
        Claimant obj = dod.getRandomClaimant();
        Assert.assertNotNull("Data on demand for 'Claimant' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Claimant' failed to provide an identifier", id);
        obj = Claimant.findClaimant(id);
        boolean modified =  dod.modifyClaimant(obj);
        Integer currentVersion = obj.getVersion();
        Claimant merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Claimant' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ClaimantIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Claimant' failed to initialize correctly", dod.getRandomClaimant());
        Claimant obj = dod.getNewTransientClaimant(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Claimant' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Claimant' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Claimant' identifier to no longer be null", obj.getId());
    }
    
}
