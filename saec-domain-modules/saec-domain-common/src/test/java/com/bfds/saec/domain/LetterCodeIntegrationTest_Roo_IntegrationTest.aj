// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.bfds.saec.domain;

import com.bfds.saec.domain.LetterCode;
import com.bfds.saec.domain.LetterCodeDataOnDemand;
import com.bfds.saec.domain.LetterCodeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect LetterCodeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: LetterCodeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: LetterCodeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: LetterCodeIntegrationTest: @Transactional;
    
    @Autowired
    private LetterCodeDataOnDemand LetterCodeIntegrationTest.dod;
    
    @Test
    public void LetterCodeIntegrationTest.testCountLetterCodes() {
        Assert.assertNotNull("Data on demand for 'LetterCode' failed to initialize correctly", dod.getRandomLetterCode());
        long count = LetterCode.countLetterCodes();
        Assert.assertTrue("Counter for 'LetterCode' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void LetterCodeIntegrationTest.testFindLetterCode() {
        LetterCode obj = dod.getRandomLetterCode();
        Assert.assertNotNull("Data on demand for 'LetterCode' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'LetterCode' failed to provide an identifier", id);
        obj = LetterCode.findLetterCode(id);
        Assert.assertNotNull("Find method for 'LetterCode' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'LetterCode' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void LetterCodeIntegrationTest.testFindAllLetterCodes() {
        Assert.assertNotNull("Data on demand for 'LetterCode' failed to initialize correctly", dod.getRandomLetterCode());
        long count = LetterCode.countLetterCodes();
        Assert.assertTrue("Too expensive to perform a find all test for 'LetterCode', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<LetterCode> result = LetterCode.findAllLetterCodes();
        Assert.assertNotNull("Find all method for 'LetterCode' illegally returned null", result);
        Assert.assertTrue("Find all method for 'LetterCode' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void LetterCodeIntegrationTest.testFindLetterCodeEntries() {
        Assert.assertNotNull("Data on demand for 'LetterCode' failed to initialize correctly", dod.getRandomLetterCode());
        long count = LetterCode.countLetterCodes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<LetterCode> result = LetterCode.findLetterCodeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'LetterCode' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'LetterCode' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void LetterCodeIntegrationTest.testFlush() {
        LetterCode obj = dod.getRandomLetterCode();
        Assert.assertNotNull("Data on demand for 'LetterCode' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'LetterCode' failed to provide an identifier", id);
        obj = LetterCode.findLetterCode(id);
        Assert.assertNotNull("Find method for 'LetterCode' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyLetterCode(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'LetterCode' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void LetterCodeIntegrationTest.testMergeUpdate() {
        LetterCode obj = dod.getRandomLetterCode();
        Assert.assertNotNull("Data on demand for 'LetterCode' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'LetterCode' failed to provide an identifier", id);
        obj = LetterCode.findLetterCode(id);
        boolean modified =  dod.modifyLetterCode(obj);
        Integer currentVersion = obj.getVersion();
        LetterCode merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'LetterCode' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void LetterCodeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'LetterCode' failed to initialize correctly", dod.getRandomLetterCode());
        LetterCode obj = dod.getNewTransientLetterCode(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'LetterCode' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'LetterCode' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'LetterCode' identifier to no longer be null", obj.getId());
    }
    
}
